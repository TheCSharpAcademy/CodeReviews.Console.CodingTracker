namespace CodingTracker
{
    internal class Enums
    {
        internal enum MenuOptions
        {
            ViewAllSessions,
            AddSession,
            UpdateSession,
            DeleteSession,
            Exit
        }
    }
}
