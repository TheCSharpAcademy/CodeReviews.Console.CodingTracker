enum SELECTOR
{
    INVALID_SELECT = -1,
    EXIT = 0,
    INSERT,
    DELETE,
    UPDATE,
    DROP,
    VIEW,
    REPORT,
    SET,
}

enum FILTER_SELECTOR
{
    INVALID_SELECT = -1,
    YEAR = 1,
    WEEK,
    DAY,
    ALL,
}